query HomeArticleListGQL($page: Int!, $filter: String) {
	homeArticleList(page: $page, filter: $filter) {
		maxPage
		homeArticleList {
			title
            slug
            excerpt
			author {
				username
				attachInfo{
					gravatar
				}
			}
			record {
				count
			}
		}
	}
}
